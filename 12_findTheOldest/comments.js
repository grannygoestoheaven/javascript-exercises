/*
    - get the age of the person for each object by substracting the year of birth form the year of death in each object
    - gather the ages in a new array
    - apply a reduce method on this array to get the oldest person
    - write the proper conditions to match the given instructions

    - if I have (acc, current), my acc will be the result of one (yearofdeath - yearofbirth) and current will be the result of the next one
    */

    // let ages = array.map(element => element.yearOfDeath - element.yearOfBirth)
    // let oldest = ages.reduce((acc, current) => Math.max(acc, current));
    // return oldest;